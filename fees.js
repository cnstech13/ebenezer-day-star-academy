import { adminReady, withTimeout } from "./admin-guard.js";
import { collection, addDoc, getDocs, query, orderBy, limit, serverTimestamp } from "https://www.gstatic.com/firebasejs/12.7.0/firebase-firestore.js";import { db } from "./firebase-config.js";import "./admin-login.js";
const form=document.getElementById("form"),rows=document.getElementById("rows"),msg=document.getElementById("msg");async function load(){const q=await withTimeout(getDocs(query(collection(db,"fees"),orderBy("createdAt","desc"),limit(100)))).catch(()=>getDocs(collection(db,"fees")));rows.innerHTML=[...q.docs].map(x=>{const d=x.data();return `<tr><td>${d.studentId||""}</td><td>${d.title||d.description||""}</td><td>₦${Number(d.amount||0).toLocaleString()}</td><td>${d.status||"Pending"}</td><td>${d.session||""}</td></tr>`}).join("")||'<tr><td colspan=5>No records.</td></tr>'}form.onsubmit=async e=>{e.preventDefault();try{await withTimeout(addDoc(collection(db,"fees"),{studentId:studentId.value.trim(),title:title.value.trim(),amount:Number(amount.value),status:status.value,session:session.value.trim(),term:term.value.trim(),createdAt:serverTimestamp()}));msg.textContent="Fee saved.";form.reset();load()}catch(e){msg.textContent=e.message}};await load();

await adminReady;
